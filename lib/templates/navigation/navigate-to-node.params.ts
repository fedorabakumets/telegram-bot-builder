/**
 * @fileoverview Параметры шаблона navigate_to_node
 * @module templates/navigation/navigate-to-node.params
 */

/** Параметры шаблона — функция статическая, параметры не нужны */
export interface NavigateToNodeTemplateParams {
  // нет параметров — функция статическая
}
