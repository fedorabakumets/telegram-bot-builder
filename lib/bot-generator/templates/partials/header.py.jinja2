{#
  Заголовок файла: UTF-8 кодировка
  
  @fileoverview Генерирует UTF-8 кодировку для Python бота
  @param {boolean} userDatabaseEnabled - Включена ли база данных пользователей (не используется, оставлен для совместимости)
  @param {boolean} hasInlineButtons - Есть ли inline кнопки (не используется, оставлен для совместимости)
  @param {boolean} hasMediaNodes - Есть ли узлы с медиа (не используется, оставлен для совместимости)
#}
# -*- coding: utf-8 -*-
import os
import sys

# Устанавливаем UTF-8 кодировку для вывода
if sys.platform.startswith("win"):
    # Для Windows устанавливаем UTF-8 кодировку
    os.environ["PYTHONIOENCODING"] = "utf-8"
    try:
        import codecs
        sys.stdout.reconfigure(encoding="utf-8")
        sys.stderr.reconfigure(encoding="utf-8")
    except (AttributeError, UnicodeError):
        # Fallback для старых версий Python
        import codecs
        sys.stdout = codecs.getwriter("utf-8")(sys.stdout.detach())
        sys.stderr = codecs.getwriter("utf-8")(sys.stderr.detach())
