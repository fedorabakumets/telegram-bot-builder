# 🚀 Деплой Telegram Bot Builder на Vercel

## 📋 Подготовка к деплою

1. Создайте аккаунт на [Vercel](https://vercel.com/)
2. Подготовьте PostgreSQL базу данных (можно использовать Supabase, Neon или другую облачную базу данных)
3. Убедитесь, что у вас есть токен для бота от [@BotFather](https://t.me/BotFather)

## 🛠️ Шаги для деплоя

### 1. Подготовка проекта

1. Зарегистрируйтесь на [Vercel](https://vercel.com/)
2. Создайте новый проект:
   - Нажмите "Add New Project"
   - Выберите "Import Git Repository" (если вы используете GitHub)
   - Или загрузите проект вручную

### 2. Настройка переменных окружения

В Vercel, перейдите в "Settings" → "Environment Variables" и добавьте следующие переменные:

```
DATABASE_URL=postgresql://username:password@host:port/database
SESSION_SECRET=your-super-secret-session-key-here-change-this-in-production
NODE_ENV=production
```

### 3. Настройка сборки

Vercel автоматически определит, что это Node.js проект, но вам нужно указать правильные команды:

1. В "Settings" → "General" → "Build & Development Settings":
   - Build Command: `npm run build`
   - Output Directory: `dist/public`
   - Install Command: `npm install`

2. В "Settings" → "Environment Variables" добавьте:
   ```
   NODE_VERSION=20
   ```

### 4. Подключение базы данных

1. Создайте базу данных на одном из сервисов:
   - [Supabase](https://supabase.com/)
   - [Neon](https://neon.tech/)
   - [Railway](https://railway.app/)
   - Или используйте Vercel Postgres (если доступно)

2. Скопируйте строку подключения DATABASE_URL
3. Добавьте её в переменные окружения вашего приложения

### 5. Запуск миграций базы данных

После первого деплоя приложения, вам нужно запустить миграции базы данных:

1. Установите Vercel CLI:
   ```bash
   npm install -g vercel
   ```

2. Зайдите в директорию проекта и выполните:
   ```bash
   vercel login
   vercel link
   ```

3. Запустите миграции:
   ```bash
   vercel exec -- npm run db:push
   ```

## 🧪 Тестирование

После успешного деплоя:
1. Откройте URL вашего приложения (отображается в "Overview")
2. Проверьте, что приложение загружается корректно
3. Попробуйте создать тестового бота

## 🛠️ Устранение неполадок

### Проблема: Ошибка `crypto.getRandomValues is not a function`

**Решение:** Мы уже добавили полифилл для этой функции в `vite.config.ts`. Убедитесь, что вы используете последнюю версию файла конфигурации.

### Проблема: Ошибка "Could not find the build directory"

**Решение:** Убедитесь, что команда сборки выполняется успешно. Проверьте логи Vercel на наличие ошибок в процессе сборки.

### Проблема: Ошибка подключения к базе данных

**Решение:**
1. Проверьте правильность строки подключения DATABASE_URL
2. Убедитесь, что база данных доступна из интернета (не локальная)
3. Проверьте, что все необходимые параметры подключения указаны

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте логи Vercel (вкладка "Logs")
2. Убедитесь, что все переменные окружения установлены правильно
3. Проверьте, что порт приложения установлен на 5000 (или используйте переменную PORT)

## 🔄 Обновление приложения

Для обновления приложения после изменений:

1. Запушьте изменения в репозиторий GitHub
2. Vercel автоматически начнет новый деплой
3. Дождитесь завершения процесса

## 📊 Мониторинг

Vercel предоставляет встроенный мониторинг:
- Просмотр логов в реальном времени
- Метрики использования CPU и памяти
- Статус здоровья приложения

## 💬 Поддержка

Если у вас возникли проблемы с деплоем:
1. Проверьте логи Vercel
2. Убедитесь, что все шаги выполнены правильно
3. Обратитесь в поддержку Vercel при необходимости