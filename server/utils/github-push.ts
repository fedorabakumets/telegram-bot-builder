import { Octokit } from '@octokit/rest';

/**
 * Асинхронная функция для взаимодействия с GitHub API
 *
 * @param token - Токен аутентификации GitHub для доступа к API
 * @returns Объект с результатом операции:
 *          - success: boolean - успешность выполнения операции
 *          - message: string - сообщение о результате операции
 *
 * @description
 * Функция создает экземпляр Octokit с предоставленным токеном аутентификации,
 * затем пытается получить информацию о репозитории 'telegram-bot-builder',
 * принадлежащем пользователю 'fedorabakumets'. Если репозиторий успешно найден,
 * функция возвращает объект с успешным результатом. В случае ошибки -
 * выбрасывает исключение.
 *
 * @example
 * // Пример использования функции
 * try {
 *   const result = await pushToGitHub('your_github_token_here');
 *   console.log(result.message); // 'Репозиторий доступен для пуша'
 * } catch (error) {
 *   console.error('Ошибка при взаимодействии с GitHub:', error);
 * }
 *
 * @throws {Error} Исключение, если произошла ошибка при взаимодействии с GitHub API
 */
export async function pushToGitHub(token: string) {
  try {
    const octokit = new Octokit({ auth: token });

    console.log('📤 Получаем статус репозитория...');
    const repo = await octokit.repos.get({
      owner: 'fedorabakumets',
      repo: 'telegram-bot-builder'
    });

    console.log('✅ Репозиторий найден:', repo.data.full_name);
    return { success: true, message: 'Репозиторий доступен для пуша' };
  } catch (error) {
    console.error('❌ Ошибка:', error);
    throw error;
  }
}
