/**
 * @fileoverview Локальный TypeScript-конфиг для папки server/utils
 * @module server/utils/tsconfig
 */
{
  "extends": "../tsconfig.json",
  "include": [
    "./**/*.ts",
    "./**/*.tsx",
    "../../shared/**/*.ts",
    "../../shared/**/*.tsx"
  ],
  "exclude": [
    "../../node_modules",
    "../../dist"
  ]
}
