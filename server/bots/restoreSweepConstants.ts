/**
 * @fileoverview Константы контрольного прохода restore (без side-effects).
 * @module server/bots/restoreSweepConstants
 */

/** Задержка до контрольного прохода после основного restore (мс) */
export const RESTORE_SWEEP_DELAY_MS = 60_000;
