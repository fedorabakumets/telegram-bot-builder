# 📝 Инструкция по обновлению проекта на GitHub

## Ваш репозиторий
🔗 https://github.com/fedorabakumets/telegram-bot-builder

---

## Способ 1: Автоматический скрипт (рекомендуется)

Откройте вкладку **Shell** в Replit и выполните:

```bash
./sync-to-github.sh "Описание ваших изменений"
```

Или просто:
```bash
./sync-to-github.sh
```
*(будет использовано автоматическое сообщение с датой и временем)*

---

## Способ 2: Вручную через Shell

Откройте вкладку **Shell** и выполните по очереди:

```bash
# 1. Настройка git
git config user.name "fedorabakumets"
git config user.email "fedorabakumets@users.noreply.github.com"

# 2. Добавление всех изменений
git add .

# 3. Создание коммита
git commit -m "Update from Replit - $(date +'%Y-%m-%d %H:%M:%S')"

# 4. Отправка на GitHub
git push origin main
```

---

## Способ 3: Через Node.js скрипт

Выполните в Shell:

```bash
npx tsx scripts/sync-github.ts
```

Этот скрипт покажет информацию о репозитории и статус изменений.

---

## Проверка статуса изменений

Чтобы посмотреть, какие файлы изменены:

```bash
git status
```

Чтобы посмотреть детали изменений:

```bash
git diff
```

---

## Последний коммит в репозитории

📅 **Дата:** 22 сентября 2025, 03:08 UTC  
👤 **Автор:** fedorabakumets  
💬 **Сообщение:** Auto-sync from Replit

---

## Возможные проблемы

### Если требуется токен доступа
1. Перейдите на https://github.com/settings/tokens
2. Создайте новый Personal Access Token
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Используйте токен вместо пароля при запросе

### Если есть конфликты
```bash
git pull origin main --rebase
# Разрешите конфликты, затем:
git push origin main
```

---

## 🎯 Быстрая команда

Для максимально быстрого обновления просто выполните:

```bash
./sync-to-github.sh
```

Готово! ✨
