# 📝 Format Module

> Модуль для форматирования текста и данных в Telegram ботах

## 📋 Содержание

- [Описание](#-описание)
- [Файлы модуля](#-файлы-модуля)
- [Функциональность](#-функциональность)
- [Примеры использования](#-примеры-использования)

## 💡 Описание

Модуль Format предоставляет набор утилит для форматирования текста, генерации безопасных имен функций, обработки специальных символов и других операций форматирования, необходимых при генерации кода Telegram ботов.

## 📁 Файлы модуля

| Файл | Назначение |
|------|------------|
| `createSafeFunctionName.ts` | Создание безопасных имен функций |
| `escapeForJsonString.ts` | Экранирование для JSON строк |
| `escapeForPython.ts` | Экранирование для Python кода |
| `formatTextForPython.ts` | Форматирование текста для Python |
| `generateButtonText.ts` | Генерация текста кнопок |
| `generateUniqueShortId.ts` | Генерация уникальных коротких ID |
| `generateWaitingStateCode.ts` | Генерация кода состояния ожидания |
| `getParseMode.ts` | Получение режима парсинга |
| `parsePythonCodeToJson.ts` | Парсинг Python кода в JSON |
| `stripHtmlTags.ts` | Удаление HTML тегов |
| `toPythonBoolean.ts` | Преобразование в Python булевый тип |
| `index.ts` | Экспорт всех функций модуля |

## 🚀 Функциональность

### 📌 formatTextForPython

Форматирует текст для безопасного использования в Python коде, экранируя специальные символы.

### 📌 createSafeFunctionName

Создает безопасные имена функций, заменяя недопустимые символы.

### 📌 escapeForJsonString

Экранирует специальные символы в строках для безопасного использования в JSON.

### 📌 escapeForPython

Экранирует специальные символы в строках для безопасного использования в Python коде.

### 📌 generateButtonText

Генерирует текст кнопки с поддержкой переменных, оборачивая их в вызов replace_variables_in_text при необходимости.

### 📌 generateUniqueShortId

Генерирует уникальные короткие идентификаторы для узлов графа на основе их полных идентификаторов.

### 📌 generateWaitingStateCode

Генерирует код установки состояния ожидания ввода от пользователя с автоматическим определением типа ввода.

### 📌 getParseMode

Преобразует формат сообщения в соответствующий параметр parse_mode для использования при отправке сообщений через Telegram API.

### 📌 parsePythonCodeToJson

Анализирует сгенерированный Python код бота и восстанавливает из него структуру узлов и соединений.

### 📌 stripHtmlTags

Удаляет HTML теги из текста для очистки форматированного текста перед отправкой через Telegram API.

### 📌 toPythonBoolean

Конвертирует JavaScript boolean значение в строковое представление Python boolean ('True' или 'False').

## 📚 Примеры использования

При генерации бота, модуль используется для форматирования текста сообщений:

```typescript
// Форматирование текста для Python
const formattedText = formatTextForPython(text);

// Создание безопасных имен функций
const safeFunctionName = createSafeFunctionName(name);

// Экранирование строк для JSON
const escapedJsonString = escapeForJsonString(text);

// Экранирование строк для Python
const escapedPythonString = escapeForPython(text);

// Генерация текста кнопки с поддержкой переменных
const buttonText = generateButtonText(text);

// Генерация уникального короткого ID
const uniqueId = generateUniqueShortId(nodeId, allNodeIds);

// Генерация кода состояния ожидания
const waitingStateCode = generateWaitingStateCode(node);

// Получение режима парсинга
const parseMode = getParseMode(format);

// Парсинг Python кода в JSON
const { nodes, connections } = parsePythonCodeToJson(pythonCode);

// Удаление HTML тегов
const cleanText = stripHtmlTags(htmlText);

// Преобразование в Python булевый тип
const pythonBool = toPythonBoolean(value);
```

---

## 🧠 Особенности

Модуль Format включает автоматическое добавление комментариев к сгенерированному коду, что улучшает читаемость и поддерживаемость.

---

<p align="center">
  Made with ❤️ for Telegram Bot Developers
</p>