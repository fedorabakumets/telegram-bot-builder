# 📝 Format Module

> Модуль для форматирования текста и данных в Telegram ботах

## 📋 Содержание

- [Описание](#-описание)
- [Файлы модуля](#-файлы-модуля)
- [Функциональность](#-функциональность)
- [Примеры использования](#-примеры-использования)

## 💡 Описание

Модуль Format предоставляет набор утилит для форматирования текста, генерации безопасных имен функций, обработки специальных символов и других операций форматирования, необходимых при генерации кода Telegram ботов.

## 📁 Файлы модуля

| Файл | Назначение |
|------|------------|
| `calculateOptimalColumns.ts` | Подсчет оптимальных колонок клавиатуры |
| `createSafeFunctionName.ts` | Создание безопасных имен функций |
| `escapeForJsonString.ts` | Экранирование для JSON строк |
| `escapeForPython.ts` | Экранирование для Python кода |
| `extractNodesAndConnections.ts` | Извлечение узлов и соединений |
| `formatTextForPython.ts` | Форматирование текста для Python |
| `generateAttachedMediaSendCode.ts` | Генерация кода отправки вложенных медиа |
| `generateButtonText.ts` | Генерация текста кнопок |
| `generateUniqueShortId.ts` | Генерация уникальных коротких ID |
| `generateWaitingStateCode.ts` | Генерация кода состояния ожидания |
| `getParseMode.ts` | Получение режима парсинга |
| `parsePythonCodeToJson.ts` | Парсинг Python кода в JSON |
| `stripHtmlTags.ts` | Удаление HTML тегов |
| `toPythonBoolean.ts` | Преобразование в Python булевый тип |
| `index.ts` | Экспорт всех функций модуля |

## 🚀 Функциональность

### 📌 formatTextForPython

Форматирует текст для безопасного использования в Python коде, экранируя специальные символы.

### 📌 createSafeFunctionName

Создает безопасные имена функций, заменяя недопустимые символы.

### 📌 calculateOptimalColumns

Вычисляет оптимальное количество колонок для клавиатуры на основе количества кнопок.

## 📚 Примеры использования

При генерации бота, модуль используется для форматирования текста сообщений:

```typescript
const formattedText = formatTextForPython(text);
const safeFunctionName = createSafeFunctionName(name);
const optimalColumns = calculateOptimalColumns(buttonsCount);
```

---

<p align="center">
  Made with ❤️ for Telegram Bot Developers
</p>