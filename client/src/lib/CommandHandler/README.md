# 🛠 Command Handler Module

> Модуль для обработки команд Telegram ботов

## 📋 Содержание

- [Описание](#-описание)
- [Файлы модуля](#-файлы-модуля)
- [Функциональность](#-функциональность)
- [Примеры использования](#-примеры-использования)

## 💡 Описание

Модуль CommandHandler предоставляет инструменты для генерации обработчиков команд Telegram ботов. Он позволяет создавать сложные сценарии взаимодействия с использованием команд, таких как `/start`, `/help` и пользовательских команд.

## 📁 Файлы модуля

| Файл | Назначение |
|------|------------|
| `generateCommandHandler.ts` | Генерация обработчиков для команд |
| `generateStartHandler.ts` | Генерация обработчика для команды /start |
| `generateCommandNodeHandlerWithKeyboardAndImageSupport.ts` | Генерация обработчика узла команды с поддержкой клавиатуры и изображений |
| `generateConditionalMessageLogicAndKeyboard.ts` | Генерация кода обработки условных сообщений и клавиатуры |
| `generateKeyboardAndProcessAttachedMedia.ts` | Генерация кода клавиатуры и обработки прикрепленных медиафайлов |
| `generateStartNodeHandlerWithConditionalLogicAndImages.ts` | Генерация обработчика узла старта с условной логикой и поддержкой изображений |
| `initializeAndRestoreMultipleSelectionState.ts` | Инициализация и восстановление состояния множественного выбора |
| `index.ts` | Экспорт всех функций модуля |

## 🚀 Функциональность

### 📌 generateCommandHandler

Генерирует обработчик для произвольной команды с учетом:

- Проверки безопасности (приватный чат, права администратора)
- Сохранения информации о пользователе
- Обработки условных сообщений
- Замены переменных в тексте

### 📌 generateStartHandler

Генерирует специализированный обработчик для команды `/start` с дополнительной логикой:

- Инициализация состояния множественного выбора
- Обработка синонимов команд
- Создание инлайн-клавиатуры для выбора опций

### 📌 generateCommandNodeHandlerWithKeyboardAndImageSupport

Генерирует обработчик узла команды с поддержкой:

- Различных типов клавиатур (inline)
- Отправки изображений вместе с сообщениями
- Корректной замены переменных в тексте сообщения
- Обработки переходов между узлами

### 📌 generateConditionalMessageLogicAndKeyboard

Обеспечивает:

- Обработку условных сообщений
- Генерацию клавиатуры (inline или reply) на основе настроек узла
- Обработку прикрепленных медиафайлов
- Формирование сообщений с учетом медиа-контента и клавиатуры

### 📌 generateKeyboardAndProcessAttachedMedia

Обеспечивает:

- Обработку условных сообщений
- Форматирование текста сообщения
- Замену переменных в тексте сообщения
- Генерацию клавиатуры для взаимодействия с пользователем

### 📌 generateStartNodeHandlerWithConditionalLogicAndImages

Обеспечивает:

- Обработку узла старт с условной логикой
- Поддержку изображений в узле
- Обработку медиа-файлов
- Универсальную замену переменных

### 📌 initializeAndRestoreMultipleSelectionState

Обеспечивает:

- Восстановление состояния множественного выбора из базы данных или локального хранилища
- Инициализацию состояния множественного выбора для пользователя
- Создание клавиатуры с восстановленными галочками для уже выбранных элементов
- Обработку кнопок интересов и другие типы кнопок

## 📚 Примеры использования

При генерации бота, модуль автоматически создаст обработчики для всех команд, определенных в узлах бота:

```typescript
const commandHandlerCode = generateCommandHandler(node, userDatabaseEnabled);
```

Для стартовой команды:

```typescript
const startHandlerCode = generateStartHandler(startNode, userDatabaseEnabled);
```

Для обработки узла команды с клавиатурой и изображениями:

```typescript
const commandNodeHandlerCode = generateCommandNodeHandlerWithKeyboardAndImageSupport(targetNode, code, actualNodeId);
```

Для инициализации и восстановления состояния множественного выбора:

```typescript
const codeLines: string[] = [];
initializeAndRestoreMultipleSelectionState(node, codeLines, userDatabaseEnabled);
```

---

<p align="center">
  Made with ❤️ for Telegram Bot Developers
</p>