/**
 * @fileoverview Реэкспорт утилит валидации токена (для обратной совместимости)
 *
 * Логика валидации перенесена в tokenUtils.ts.
 * Этот файл сохранён для обратной совместимости.
 *
 * @module tokenValidation
 */

export { validateBotToken } from './tokenUtils';
