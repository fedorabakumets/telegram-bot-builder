# 🧩 Conditional Module

> Модуль для условной логики и обработки условных сообщений в Telegram ботах

## 📋 Содержание

- [Описание](#-описание)
- [Файлы модуля](#-файлы-модуля)
- [Функциональность](#-функциональность)
- [Примеры использования](#-примеры-использования)

## 💡 Описание

Модуль Conditional предоставляет инструменты для реализации условной логики в Telegram ботах. Он позволяет создавать сложные сценарии с ветвлениями на основе различных условий и данных пользователя.

## 📁 Файлы модуля

| Файл | Назначение |
|------|------------|
| `collectConditionalMessageButtons.ts` | Сбор условных кнопок сообщений |
| `conditional-button-handler.ts` | Обработка условных кнопок |
| `generateConditionalKeyboard.ts` | Генерация условных клавиатур |
| `generateConditionalMessageLogic.ts` | Генерация логики условных сообщений |
| `generateMessageNodeHandlerWithConditionalLogicAndMediaSupport.ts` | Генерация обработчика узла сообщения с поддержкой условной логики и медиафайлов |
| `hasConditionalButtons.ts` | Проверка наличия условных кнопок в узлах |
| `processConditionalMessages.ts` | Обработка условных сообщений |
| `index.ts` | Экспорт всех функций модуля |

## 🚀 Функциональность

### 📌 collectConditionalMessageButtons

Собирает все условные кнопки из сообщений для дальнейшей обработки и создания обработчиков.

### 📌 generateConditionalMessageLogic

Генерирует Python код для обработки условных сообщений, включая:

- Проверку условий
- Ветвления логики
- Замену переменных
- Обработку синонимов

### 📌 conditional-button-handler

Обработка условных кнопок с учетом состояния пользователя и контекста.

### 📌 generateConditionalKeyboard

Генерирует условные клавиатуры для интерактивного взаимодействия с пользователем на основе условий.

### 📌 generateMessageNodeHandlerWithConditionalLogicAndMediaSupport

Генерирует обработчик узла сообщения с поддержкой условной логики и медиафайлов, позволяя создавать сложные сценарии с вложениями.

### 📌 hasConditionalButtons

Проверяет наличие условных кнопок в узлах, что позволяет определить, требует ли узел специальной обработки.

### 📌 processConditionalMessages

Обрабатывает массив условных сообщений и генерирует соответствующий Python-код для обработки различных условий.

## 📚 Примеры использования

При генерации бота, модуль автоматически создаст условную логику для узлов с условными сообщениями:

```typescript
// Генерация условной логики
const conditionalLogic = generateConditionalMessageLogic(conditionalMessages, indent);

// Проверка наличия условных кнопок
const hasConditionalBtns = hasConditionalButtons(nodes);

// Обработка условных сообщений
const processedCode = processConditionalMessages(sortedConditions, nodeData, code, indentLevel);

// Генерация обработчика узла с поддержкой медиа и условной логики
const handlerCode = generateMessageNodeHandlerWithConditionalLogicAndMediaSupport(targetNode, code, allNodeIds, connections, mediaVariablesMap, actualNodeId);
```

---

## 🧠 Особенности

Модуль Conditional включает автоматическое добавление комментариев к сгенерированному коду, что улучшает читаемость и поддерживаемость.

---

<p align="center">
  Made with ❤️ for Telegram Bot Developers
</p>