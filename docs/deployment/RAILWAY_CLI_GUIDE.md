# 🚀 Railway CLI - Быстрый деплой

## 📦 Установка Railway CLI

```bash
# Установка глобально
npm install -g @railway/cli

# Проверка версии
railway --version
```

## 🔐 Авторизация

```bash
# Вход в аккаунт (откроет браузер)
railway login
```

## 🚀 Деплой проекта

### Вариант 1: Через CLI (быстрый)
```bash
# 1. Инициализация проекта
railway init

# 2. Добавление PostgreSQL
railway add --database postgres

# 3. Настройка переменных
railway variables set SESSION_SECRET=your-secret-key-here
railway variables set NODE_ENV=production

# 4. Деплой
railway up

# 5. Открыть в браузере
railway open
```

### Вариант 2: Через веб-интерфейс (надежный)
1. Иди на [railway.app](https://railway.app)
2. **New Project** → **Deploy from GitHub repo**
3. Выбери репозиторий → **Deploy Now**
4. **+ New** → **Database** → **Add PostgreSQL**
5. **Variables** → добавь переменные

## 🔧 Полезные команды

```bash
# Просмотр логов в реальном времени
railway logs

# Статус проекта
railway status

# Список переменных
railway variables

# Подключение к базе данных
railway connect postgres

# Открыть проект в браузере
railway open

# Локальная разработка с Railway переменными
railway run npm run dev
```

## 🛠️ Настройка переменных через CLI

```bash
# Основные переменные
railway variables set SESSION_SECRET=railway-super-secret-key-12345
railway variables set NODE_ENV=production

# Опциональные
railway variables set BOT_RUNNER_URL=https://your-bot-runner.railway.app
```

## 📊 Мониторинг

```bash
# Логи в реальном времени
railway logs --follow

# Статус сервисов
railway status

# Информация о проекте
railway info
```

## 🔄 Обновление приложения

```bash
# После изменений в коде
git add .
git commit -m "Update"
git push

# Или прямой деплой
railway up
```

## 🆘 Решение проблем

### Ошибка "No services found"
```bash
# Создай сервис через веб-интерфейс, затем:
railway link
```

### Ошибка деплоя
```bash
# Проверь логи
railway logs

# Проверь статус
railway status
```

### Проблемы с сетью
- Используй веб-интерфейс как альтернативу
- Проверь интернет-соединение
- Попробуй позже

## 🎯 Рекомендуемый workflow

1. **Разработка**: `railway run npm run dev` (с Railway переменными)
2. **Тестирование**: локально с `.env`
3. **Деплой**: `railway up` или через GitHub
4. **Мониторинг**: `railway logs --follow`

---

**💡 Совет**: Если CLI глючит, всегда можно использовать веб-интерфейс Railway - он более стабильный.