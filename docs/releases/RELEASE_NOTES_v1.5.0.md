# 📦 Telegram Bot Builder v1.5.0

**Дата выпуска:** 21 февраля 2026 г.

**Сравнение версий:** [v1.4.4...v1.5.0](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.4.4...v1.5.0)

---

## 🎯 Основные изменения

### ✨ TELEGRAM CLIENT API — РАБОТА С TELEGRAM ОТ ЛИЦА ПОЛЬЗОВАТЕЛЯ

🆕 **Полноценная интеграция Telegram Client API** для взаимодействия с Telegram на уровне пользователя

#### Ключевые возможности:

- **Авторизация через QR-код** — быстрая и удобная аутентификация в Telegram
- **Авторизация по SMS** — классический вход с кодом подтверждения
- **Участники групп** — просмотр и управление участниками групп
- **Отправка сообщений** — отправка сообщений от имени пользователя

#### Компоненты:

- **`TelegramClientConfig.tsx`** — основной компонент настройки Telegram Client
  - Настройка API ID и API Hash
  - Выбор метода авторизации (QR/SMS)
  - Статус подключения
  - Управление сессией

- **`GroupMembersClientPanel.tsx`** — панель работы с участниками групп
  - Список участников
  - Фильтрация и поиск
  - Статистика группы

- **`qr-auth-error-handler.ts`** — обработка ошибок QR-авторизации
  - Истёкший QR-код
  - Ошибка сети
  - Неверные учётные данные

- **`telegram-auth-methods.ts`** — методы авторизации
  - QR-код генерация
  - SMS код запрос
  - Проверка статуса

- **`telegram-auth-service.ts`** — сервис авторизации
  - Управление сессиями
  - Хранение токенов
  - Валидация пользователя

---

### 📬 РАССЫЛКА ПО ЛИЧНЫМ СООБЩЕНИЯМ

🆕 **Система массовой рассылки сообщений** пользователям в личные сообщения

#### Возможности:

- **Broadcast-узлы** — специальный тип узла для рассылки
- **База данных User IDs** — хранение и управление ID пользователей
- **Сохранение ID пользователей** — автоматический сбор ID при взаимодействии
- **Отправка сообщений** — массовая отправка в ЛС
- **Статистика рассылки** — отслеживание результатов

#### Компоненты:

- **`broadcast-node.tsx`** — узел рассылки в редакторе
  - Настройка сообщения
  - Выбор получателей
  - Предпросмотр

- **`broadcast-properties.tsx`** — панель свойств рассылки
  - Тип API (Client/Bot)
  - Источник ID пользователей
  - Опции сохранения ID

- **`broadcast-toggle.tsx`** — переключатель режима рассылки
  - Включение/выключение
  - Индикация статуса

- **`api-type-selector.tsx`** — выбор типа API
  - Telegram Bot API
  - Telegram Client API

- **`id-source-selector.tsx`** — выбор источника ID
  - Из базы данных
  - Из переменной
  - Ручной ввод

- **`save-to-user-ids-switch.tsx`** — переключатель сохранения ID
  - Автоматическое сохранение
  - Обновление существующих

#### База данных User IDs:

- **`user-ids-database.tsx`** — основной компонент управления базой
  - Список всех ID
  - Поиск и фильтрация
  - Экспорт/импорт

- **`user-id-list.tsx`** — отображение списка ID
  - Пагинация
  - Сортировка
  - Статус каждого ID

- **`user-id-actions.tsx`** — действия с ID
  - Добавление
  - Удаление
  - Редактирование

- **`user-id-stats.tsx`** — статистика базы
  - Общее количество
  - Активные/неактивные
  - Дата добавления

---

### 🔧 ГЕНЕРАЦИЯ КОДА РАССЫЛКИ

🆕 **Модули генерации Python-кода** для рассылки сообщений

#### Генераторы:

- **`generateBroadcastHandler.ts`** — генерация обработчика рассылки через Bot API
  - Отправка сообщений ботом
  - Обработка ошибок
  - Логирование результатов

- **`generateBroadcastClientHandler.ts`** — генерация обработчика через Client API
  - Отправка от имени пользователя
  - Работа с сессиями
  - Поддержка разных аккаунтов

- **`generateSaveToUserIds.ts`** — генерация кода сохранения ID
  - Автоматическое сохранение
  - Проверка дубликатов
  - Обновление записей

- **`generateDatabaseVariables.ts`** — генерация переменных базы данных
  - Подключение к БД
  - Создание таблиц
  - Миграции

---

### 🗄️ МИГРАЦИИ БАЗЫ ДАННЫХ

🆕 **Новые таблицы для хранения User IDs**

#### Миграции:

- **`0003_add_user_ids_table.sql`** — создание таблицы `user_ids`
  ```sql
  CREATE TABLE user_ids (
      id SERIAL PRIMARY KEY,
      user_id BIGINT NOT NULL,
      username TEXT,
      first_name TEXT,
      last_name TEXT,
      created_at TIMESTAMP DEFAULT NOW(),
      updated_at TIMESTAMP DEFAULT NOW()
  );
  ```

- **`0004_add_user_ids_unique_constraint.sql`** — уникальность user_id
  ```sql
  ALTER TABLE user_ids ADD CONSTRAINT unique_user_id UNIQUE (user_id);
  ```

#### Серверные обработчики:

- **`user-ids-handler.ts`** — обработка запросов к базе ID
  - CRUD операции
  - Валидация данных
  - Обработка ошибок

- **`user-ids-routes.ts`** — API маршруты
  - GET /api/user-ids — список всех ID
  - POST /api/user-ids — добавление ID
  - DELETE /api/user-ids/:id — удаление ID
  - PUT /api/user-ids/:id — обновление ID

---

### 🎨 UI/UX УЛУЧШЕНИЯ

#### Новый компонент QR-кода:

- **`qr-code-generator.tsx`** — генерация QR-кодов для авторизации
  - Динамическое обновление
  - Анимация сканирования
  - Статус подключения

#### Улучшенная авторизация:

- **`telegram-sms-resend.tsx`** — компонент повторной отправки SMS
  - Таймер обратного отсчёта
  - Кнопка resend
  - Статус отправки

- **`use-telegram-resend-code.ts`** — хук управления повторной отправкой
  - Debounce запросов
  - Обработка ошибок
  - Состояние загрузки

#### Обновлённый заголовок:

- **`adaptive-header.tsx`** — новая навигация
  - Вкладка "Client API"
  - Вкладка "Рассылка"
  - Адаптивный дизайн

---

## 📊 Статистика изменений

| Категория | Файлов | Строк добавлено | Строк удалено |
|-----------|--------|-----------------|---------------|
| Telegram Client API | 12 | 1,847 | 148 |
| Рассылка (Broadcast) | 8 | 1,156 | 0 |
| База данных User IDs | 6 | 549 | 0 |
| Генерация кода | 5 | 851 | 0 |
| Миграции БД | 2 | 35 | 0 |
| UI/UX улучшения | 4 | 354 | 0 |
| **Всего** | **37** | **4,792** | **148** |

---

## 🔧 Технические детали

### Архитектура Telegram Client API

```
client/components/editor/telegram-client/
├── TelegramClientConfig.tsx    # Настройка клиента
├── GroupMembersClientPanel.tsx # Работа с группами
├── README.ts                   # Документация
├── index.ts                    # Экспорты
└── test-config.ts              # Тестовые данные
```

### Система авторизации

```
server/telegram/
├── telegram-auth-service.ts    # Сервис авторизации
├── telegram-auth-methods.ts    # Методы (QR/SMS)
├── qr-auth-error-handler.ts    # Обработка ошибок
└── telegram-client.ts          # Клиент Telegram
```

### Модули рассылки

```
client/components/editor/properties/
├── broadcast-node.tsx          # Узел рассылки
├── broadcast-properties.tsx    # Свойства
├── broadcast-toggle.tsx        # Переключатель
├── api-type-selector.tsx       # Выбор API
├── id-source-selector.tsx      # Источник ID
└── save-to-user-ids-switch.tsx # Сохранение ID
```

### Генерация кода

```
client/lib/generate/
├── generateBroadcastHandler.ts       # Bot API handler
├── generateBroadcastClientHandler.ts # Client API handler
├── generateSaveToUserIds.ts          # Сохранение ID
└── generateDatabaseVariables.ts      # Переменные БД
```

### База данных User IDs

```
client/components/editor/user-ids-db/
├── user-ids-database.tsx   # Основной компонент
├── user-id-list.tsx        # Список ID
├── user-id-actions.tsx     # Действия
├── user-id-stats.tsx       # Статистика
└── index.ts                # Экспорты
```

---

## 📝 Примеры использования

### Настройка Telegram Client API

```typescript
// В редакторе проекта
<TelegramClientConfig
  apiId="YOUR_API_ID"
  apiHash="YOUR_API_HASH"
  authMethod="qr" // или "sms"
  onAuthComplete={(session) => {
    // Сессия сохранена
  }}
/>
```

### Создание рассылки

```typescript
// Broadcast-узел в сценарии
<BroadcastNode
  apiType="client" // или "bot"
  idSource="database" // или "variable", "manual"
  message="Привет! Это рассылка."
  saveToUserIds={true}
/>
```

### Работа с базой ID

```typescript
// Добавление пользователя
await fetch('/api/user-ids', {
  method: 'POST',
  body: JSON.stringify({
    user_id: 123456789,
    username: '@username',
    first_name: 'Имя'
  })
});
```

---

## 🚀 Установка

```bash
# Обновите проект
git pull origin main

# Установите зависимости
cd client
npm install

# Примените миграции БД
npm run db:migrate

# Запустите проект
npm run dev
```

---

## ⚠️ Важные замечания

### Telegram Client API

- **API ID и API Hash** — получите на https://my.telegram.org
- **Безопасность** — храните учётные данные в секрете
- **Сессии** — сохраняются на сервере, требуют защиты
- **Лимиты** — соблюдайте лимиты Telegram API

### Рассылка

- **Спам** — не рассылайте спам, уважайте пользователей
- **Согласие** — получайте согласие на рассылку
- **Лимиты** — Telegram ограничивает массовые рассылки
- **Блокировки** — при злоупотреблении возможна блокировка

### База данных

- **Резервное копирование** — регулярно делайте бэкапы
- **Миграции** — применяйте миграции перед запуском
- **Индексы** — добавлены индексы для производительности

---

## 📦 Зависимости

### Новые зависимости:

```json
{
  "telegraf": "^4.x",
  "qr-code-styling": "^1.x"
}
```

### Обновлённые зависимости:

- `react-query` — обновлено для поддержки новых хуков
- `drizzle-orm` — обновлено для новых миграций

---

## 🔗 Ссылки

- [Исходный код релиза](https://github.com/fedorabakumets/telegram-bot-builder/tree/v1.5.0)
- [Сравнение изменений](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.4.4...v1.5.0)
- [Полный список коммитов](https://github.com/fedorabakumets/telegram-bot-builder/commits/v1.5.0)
- [Документация Telegram Client API](client/components/editor/telegram-client/README.ts)

---

**Полный список изменений:** [Коммиты v1.4.4...v1.5.0](https://github.com/fedorabakumets/telegram-bot-builder/commits/v1.5.0)
