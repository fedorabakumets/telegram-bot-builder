# 📦 Telegram Bot Builder v1.5.1

**Дата выпуска:** 25 февраля 2026 г.

**Сравнение версий:** [v1.5.0...v1.5.1](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.5.0...v1.5.1)

---

## 🎯 Основные изменения

### ✨ Кнопка «Применить» в панели свойств

Добавлена кнопка явного сохранения изменений в панели свойств узла.

#### Возможности:

- **Явное сохранение** — кнопка «Применить» сохраняет проект на сервер
- **Уведомление** — всплывающее сообщение об успешном сохранении
- **Логирование** — действие сохраняется в истории действий
- **Автозакрытие** — уведомление исчезает через 1 секунду

#### Компоненты:

- **`client/components/editor/properties/apply-button.tsx`** — новый компонент кнопки
- **`client/components/editor/properties/properties-footer.tsx`** — обновлён футер панели
- **`client/pages/editor.tsx`** — функция `handleSaveProject` для сохранения

---

### 📬 Улучшения системы рассылок (Broadcast)

Полный рефакторинг модуля генерации рассылки для стабильности.

#### Изменения:

- Разделение на подпапки **BotApi** и **Client**
- Исправление обработки путей к медиафайлам
- Улучшена работа с абсолютными и относительными путями
- Исправлена функция `get_full_media_path` для Telethon

---

### 🔧 Конфигурация через .env

Вынесены настройки в переменные окружения для удобства развёртывания.

#### Новые переменные:

- **`BOT_TOKEN`** — токен бота в отдельном .env файле (для сгенерированных ботов)
- **`ADMIN_IDS`** — список администраторов (формат в столбик)
- **`ENABLE_LOGS`** — включение логирования
- **`DEBUG_MODE`** — режим отладки

#### Изменения:

- Удалена генерация `config.yaml` — все настройки через .env
- Файл `bot_data.json` переименован в `project.json` (вкладка «Код»)
- Динамические имена файлов в генераторе кода

---

### 🎨 Рефакторинг UI компонентов

Улучшена структура кода для лучшей поддерживаемости интерфейса.

#### Управление ботами (`BotControl`):

- Выделены отдельные компоненты для карточки бота
- Диалог добавления бота разбит на модули
- Диалог редактирования токена разбит на компоненты
- Терминал разбит на 6 хуков и отдельные компоненты

#### Панель свойств:

- Сетка настроек бота выделена в отдельный компонент
- Кнопки действий вынесены в отдельные модули
- Удалён неиспользуемый код

---

### 📚 README генератор

Улучшена генерация документации для созданных ботов.

#### Возможности:

- **Превью README** — предпросмотр с разделённой панелью
- **Monaco Editor** — редактирование слева, превью справа
- **Команды BotFather** — красивые блоки с кнопками копирования
- **Карточки статистики** — вставляются в нужные секции
- **Синхронизация прокрутки** — редактор и превью синхронизированы

#### Компоненты:

- **`client/components/editor/code/readme-preview.tsx`** — превью README
- **`client/lib/generate/generateReadme.ts`** — генерация документации

---

### ⌨️ Горячие клавиши

Добавлена поддержка горячих клавиш для удобной работы.

#### Комбинации:

- **Ctrl+Z** — отменить действие (undo)
- **Ctrl+Y / Ctrl+Shift+Z** — повторить действие (redo)
- **Ctrl+C** — копировать узел
- **Ctrl+V** — вставить узел
- **Ctrl+Shift+C/V** — расширенное копирование/вставка

#### Особенности:

- Поддержка русской и английской раскладок
- Логирование действий в историю
- Корректная работа undo/redo для всех действий

---

### 🎯 Вставка узлов в позицию клика

Улучшено позиционирование вставляемых узлов.

#### Изменения:

- Точная вставка в точку клика мыши
- Учёт масштаба (zoom) и панорамирования (pan)
- Правильная конвертация координат экрана в координаты холста
- Центрирование узла относительно курсора

---

### 🐛 Исправления ошибок

#### Telegram Client API:

- Обработка ошибок соединения с базой данных
- Проверка `result.client` на undefined
- Исправление `isActive: true` → `isActive: 1` для совместимости
- Укорочение ID узлов при дублировании листов

#### TypeScript:

- Исправлены ошибки в `telegram-client.ts`
- Улучшена доступность UI компонентов
- Удалены неиспользуемые импорты и переменные

#### Python код генерации:

- Исправление операторов `&&` → `and`, `||` → `or`
- Исправление `startsWith` → `startswith`
- Сохранение отступов при генерации кода

---

## 📊 Статистика изменений

| Категория | Файлов | Строк добавлено | Строк удалено |
|-----------|--------|-----------------|---------------|
| Кнопка «Применить» | 4 | 110 | 25 |
| Рефакторинг Broadcast | 8 | 50 | 30 |
| Конфигурация .env | 6 | 80 | 150 |
| Рефакторинг UI | 25 | 400 | 350 |
| README генератор | 4 | 200 | 50 |
| Горячие клавиши | 6 | 150 | 80 |
| Вставка узлов | 3 | 100 | 40 |
| Исправления | 15 | 200 | 180 |
| **Всего** | **71** | **1290+** | **905+** |

---

## 🔧 Технические детали

### Кнопка «Применить»

```typescript
// client/components/editor/properties/apply-button.tsx
export function ApplyButton({ onNodeUpdate, onSaveProject, onActionLog }) {
  const handleApply = () => {
    if (onActionLog) onActionLog('apply', 'Применены изменения');
    if (onNodeUpdate) onNodeUpdate();
    if (onSaveProject) onSaveProject();
    toast({ title: 'Изменения применены', duration: 1000 });
  };
}
```

### Горячие клавиши

```typescript
// Обработка Ctrl+Z/Ctrl+Y
document.addEventListener('keydown', (e) => {
  if ((e.ctrlKey || e.metaKey) && e.key === 'z') {
    handleUndo();
  }
  if ((e.ctrlKey || e.metaKey) && (e.key === 'y' || e.key === 'z' && e.shiftKey)) {
    handleRedo();
  }
});
```

### Вставка узлов

```typescript
// Конвертация координат экрана в координаты холста
const screenToCanvas = (screenX: number, screenY: number) => {
  const rect = canvasRef.current.getBoundingClientRect();
  const x = (screenX - rect.left - pan.x) / zoom;
  const y = (screenY - rect.top - pan.y) / zoom;
  return { x, y };
};
```

---

## 🚀 Установка

### Обновление с v1.5.0:

```bash
# Обновите проект
git pull origin main

# Установите зависимости
cd client
npm install

# Соберите проект
npm run build

# Запустите
npm run start
```

### Через Docker:

```bash
# Пересоберите образ
docker-compose build --no-cache

# Запустите
docker-compose up -d
```

---

## ⚠️ Важные замечания

### Конфигурация

- ** .env файл** — проверьте наличие всех переменных окружения
- **ADMIN_IDS** — формат изменён на построчный
- **Миграции** — новые миграции не требуются

### Горячие клавиши

- **Раскладка** — поддерживаются русская и английская
- **Конфликты** — проверьте отсутствие конфликтов с браузером

### README генератор

- **Превью** — доступно на вкладке «Код»
- **Синхронизация** — прокрутка синхронизирована автоматически

---

## 📦 Зависимости

Новые зависимости не требуются.

---

## 🔗 Ссылки

- [Исходный код релиза](https://github.com/fedorabakumets/telegram-bot-builder/tree/v1.5.1)
- [Сравнение изменений](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.5.0...v1.5.1)
- [Полный список коммитов](https://github.com/fedorabakumets/telegram-bot-builder/commits/v1.5.1)

---

## 📋 Чек-лист обновления

- [x] Кнопка «Применить» реализована
- [x] Рефакторинг Broadcast завершён
- [x] Конфигурация через .env обновлена
- [x] UI компоненты рефакторены
- [x] README генератор добавлен
- [x] Горячие клавиши реализованы
- [x] Вставка узлов улучшена
- [x] Ошибки исправлены

---

**Полный список изменений:** [Коммиты v1.5.0...v1.5.1](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.5.0...v1.5.1)
