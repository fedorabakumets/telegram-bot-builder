## 🔧 Исправления

### Конфигурация Google OAuth

- 📁 **Перемещены файлы** `credentials.json` и `token.json` в `config/google-auth/`
- 🔐 **Обновлены пути** поиска конфигов (обратная совместимость сохранена)
- 📝 **Добавлена документация** в `config/google-auth/README.md`
- ✅ **Файлы добавлены** в `.gitignore`

### Обработка ошибок

- 🤫 **Тихая обработка** ошибки аутентификации OAuth (не спамит в логах)
- ⚠️ **Возврат 401** с `requiresAuth: true` для клиента

### Кэширование и обновление данных

- 🔄 **Исправлено обновление времени экспорта** в UI
- 🗑️ **Инвалидация кэша** проекта после экспорта (`dbCache.delete`)
- ⚡ **HTTP заголовки** `no-cache` для `GET /api/projects/:id`
- 🔍 **React Query** `invalidateQueries` после экспорта

---

## ✅ Оба типа экспорта работают корректно

| Тип экспорта | Статус |
|-------------|--------|
| Экспорт пользователей | ✅ Время обновляется |
| Экспорт структуры | ✅ Время обновляется |

---

## 🔧 Технические изменения

### Изменённые файлы

- `server/google-sheets/auth.ts` — новые пути для credentials/token
- `server/google-sheets/export-metadata.ts` — инвалидация кэша
- `server/routes/setupProjectRoutes.ts` — HTTP заголовки, type='userDatabase'
- `client/src/components/editor/GoogleSheetsExportButton.tsx` — invalidateQueries
- `client/src/components/editor/code-panel.tsx` — invalidateQueries
- `.gitignore` — новые пути для конфигов
- `config/google-auth/README.md` — документация

### Исправления

- Ключ кэша `'project:30'` не совпадал с паттерном `'project:30:.*'`
- HTTP кэш `304 Not Modified` на GET запросах
- Токен сохранялся в старую папку

---

## 📦 Установка

Обновите проект до последней версии:

```bash
git pull origin main
```

Если используете Google OAuth, переместите файлы:

```bash
# Старые расположения (можно удалить после проверки)
client/src/components/editor/credentials.json
client/src/components/editor/token.json

# Новое расположение
config/google-auth/credentials.json
config/google-auth/token.json
```

---

**Сравнение версий:** [v1.2.0...v1.2.1](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.2.0...v1.2.1)
