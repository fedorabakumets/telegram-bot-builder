# 📦 Telegram Bot Builder v1.4.2

**Дата выпуска:** 19 февраля 2026 г.

**Сравнение версий:** [v1.4.1...v1.4.2](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.4.1...v1.4.2)

---

## 🎯 Основные изменения

### ⚡ РЕАЛИЗАЦИЯ АВТОПЕРЕХОДОВ МЕЖДУ УЗЛАМИ

🆕 **Полная реализация автоматических переходов** между узлами бота после отправки сообщения

#### Ключевые изменения:

- **`generateStartHandler.ts`** — добавлен класс `FakeMessageEdit` для вызова callback-обработчиков при автопереходе из start-узлов
  - Атрибуты: `from_user`, `chat`, `date`, `message_id`, `data`, `message`
  - Методы: `answer()`, `edit_text()`
  - Поддержка навигации через callback_data

- **`generateStartNodeHandlerWithConditionalLogicAndImages.ts`** — добавлена логика автоперехода для callback-обработчиков start-узлов
  - Проверка существования целевого узла
  - Обработка ошибок перехода
  - Логирование выполнения автоперехода

- **`generateHandleNodeFunctions.ts`** — исправлена реализация автоперехода для узлов с `collectUserInput=true`
  - Вызов `handle_node_*` обработчиков
  - Проверка условий для выполнения перехода
  - Fallback на сообщение об ошибке

#### Пример использования:

```python
# В сгенерированном Python-коде
if node.data.enableAutoTransition and node.data.autoTransitionTo:
    logging.info(f"⚡ Автопереход от узла {node.id} к узлу {autoTargetId}")
    await handle_callback_{safeFuncName}(fake_edit_message)
    logging.info(f"✅ Автопереход выполнен: {node.id} -> {autoTargetId}")
```

---

### 🐛 ИСПРАВЛЕНИЯ ГЕНЕРАТОРА PYTHON-КОДА

#### Исправление проблем с кодировкой и импортами:

- **Инициализация callback_data** — добавлена проверка перед использованием в callback-обработчиках
- **Замена кавычек** — двойные кавычки заменены на одинарные для `user_name` в строковых литералах
- **Перемещение import re** — перемещён в начало файла для корректной работы
- **Замена locals() на try/except** — улучшена обработка замены переменных
  - Более надёжная обработка отсутствующих переменных
  - Уменьшение количества ошибок выполнения

#### Устранение дублирования кода:

- **`generateKeyboard.ts`** — удалено дублирование вызова `generateUniversalVariableReplacement`
  - Оптимизация генерации кода
  - Уменьшение размера сгенерированных файлов

- **`generateBaseCallbackHandlerStructure.ts`** — добавлен импорт `ParseMode`
  - Поддержка markdown-форматирования в узлах

- **Синхронизация генерации комментариев** — обновлена логика добавления авто-комментариев между серверными файлами

---

### 🎨 UI-УЛУЧШЕНИЯ

#### Временно скрыта кнопка "Группы"

- **`adaptive-header.tsx`** — закомментирована кнопка "Группы" в навигации
  - Desktop навигация
  - Мобильная навигация
  - Может быть возвращена раскомментированием строк

#### Выделена кнопка сохранения проекта

- **`canvas.tsx`** — обновлён стиль кнопки "Сохранить проект"
  - Градиентный синий фон: `blue-600 → blue-700`
  - Белая иконка для контраста
  - Синяя тень `shadow-blue-600/30`
  - Усиленный hover-эффект
  - **Тёмная тема:** `dark:blue-700 → dark:blue-800`

---

## 📊 Статистика изменений

| Категория | Файлов | Строк добавлено | Строк удалено |
|-----------|--------|-----------------|---------------|
| Реализация автопереходов | 3 | 77 | 22 |
| Исправления генератора | 7 | 31 | 15 |
| UI-улучшения | 2 | 12 | 12 |
| **Всего** | **12** | **120** | **49** |

---

## 🔧 Технические детали

### Класс FakeMessageEdit

```python
class FakeMessageEdit:
    def __init__(self, message, target_node_id):
        self.from_user = message.from_user
        self.chat = message.chat
        self.date = message.date
        self.message_id = message.message_id
        self.data = target_node_id  # callback_data для навигации
        self.message = message  # ссылка на оригинальное сообщение
        self._message = message
    
    async def answer(self, text, parse_mode=None, reply_markup=None):
        await self._message.answer(text, parse_mode=parse_mode, reply_markup=reply_markup)
    
    async def edit_text(self, text, parse_mode=None, reply_markup=None):
        await self._message.edit_text(text, parse_mode=parse_mode, reply_markup=reply_markup)
```

### Изменения в файлах

#### `client/lib/CommandHandler/generateStartHandler.ts`
- Добавлен класс `FakeMessageEdit`
- Реализован автопереход для start-узлов
- Обработка ошибок и логирование

#### `client/lib/CommandHandler/generateStartNodeHandlerWithConditionalLogicAndImages.ts`
- Добавлена логика автоперехода
- Проверка существования целевого узла

#### `client/lib/generate/generateHandleNodeFunctions.ts`
- Исправлена заглушка на реальный вызов обработчиков
- Улучшено логирование

#### `client/components/editor/canvas/canvas.tsx`
- Обновлён стиль кнопки сохранения
- Синий градиент для выделения

#### `client/components/layout/adaptive-header.tsx`
- Временно скрыта кнопка "Группы"

---

## 🧹 Дополнительные исправления

### Исправления в генераторе ботов:

- **`bot-generator.ts`** — улучшена обработка callback_data
- **`generateKeyboard.ts`** — оптимизация замены переменных
- **`generateMultiSelectDoneHandler.ts`** — исправление импортов
- **`generateUniversalVariableReplacement.ts`** — замена locals() на try/except

### Серверные исправления:

- **`startBot.ts`** — синхронизация логики комментариев

---

## 🎨 UI/UX улучшения

1. **Кнопка сохранения** — теперь заметнее благодаря синему градиенту
2. **Тёмная тема** — полная поддержка для всех новых элементов
3. **Навигация** — возможность временного скрытия ненужных кнопок

---

## 🚀 Установка

Если вы используете локальную копию проекта:

```bash
# Обновите проект
git pull origin main

# Установите зависимости
npm install

# Соберите проект
npm run build

# Запустите проект
npm run start
```

---

## 📝 Примечания

- ✅ **Обратная совместимость** — все изменения обратно совместимы
- ✅ **Тестирование** — изменения протестированы на корректную работу
- 📦 **Зависимости** — не требуются новые зависимости
- ⚠️ **Кнопка "Группы"** — временно скрыта, может быть возвращена раскомментированием

---

## 🔗 Ссылки

- [Исходный код релиза](https://github.com/fedorabakumets/telegram-bot-builder/tree/v1.4.2)
- [Сравнение изменений](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.4.1...v1.4.2)
- [Полный список коммитов](https://github.com/fedorabakumets/telegram-bot-builder/commits/v1.4.2)

---

## 📋 Чек-лист обновления

- [x] Реализованы автопереходы между узлами
- [x] Исправлены проблемы генератора Python-кода
- [x] Улучшен UI кнопки сохранения
- [x] Временно скрыта кнопка "Группы"
- [x] Обновлена документация
- [x] Протестировано в светлой и тёмной темах

---

**Полный список изменений:** [Коммиты v1.4.1...v1.4.2](https://github.com/fedorabakumets/telegram-bot-builder/commits/main)
