# 📦 Telegram Bot Builder v1.4.3

**Дата выпуска:** 19 февраля 2026 г.

**Сравнение версий:** [v1.4.2...v1.4.3](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.4.2...v1.4.3)

---

## 🎯 Основные изменения

### 🐛 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Генерация handle_callback_start для навигации

Исправлена критическая ошибка в генераторе Python-кода, которая приводила к ошибке `"handle_callback_start" не определено` при использовании навигации на узел `start`.

#### Проблема:

Когда другие узлы ссылались на узел `start` (через кнопки skipDataCollection, автопереходы и т.д.), генератор создавал вызов `await handle_callback_start(...)`, но сама функция не генерировалась.

**Причина:** В генераторе была проверка, которая ошибочно предполагала, что `start_handler` и `handle_callback_start` — одна и та же функция.

#### Решение:

- ✅ **Создаются ОБЕ функции:**
  - `start_handler` — обрабатывает команду `/start` (message handler)
  - `handle_callback_start` — обрабатывает навигацию через callback (callback handler)

- ✅ **Убрана проверка** в `newgenerateInteractiveCallbackHandlersWithConditionalMessagesMultiSelectAndAutoNavigation.ts`, которая пропускала создание `handle_callback_start`

- ✅ **Исправлена ошибка** при использовании кнопок skipDataCollection и автопереходов на узел start

#### Пример сценария, где возникала ошибка:

```
[Узел A] --(автопереход)--> [Узел start]
                            ↓
                    Ошибка: handle_callback_start не определено
```

**После исправления:**
```
[Узел A] --(автопереход)--> [Узел start]
                            ↓
                    ✅ handle_callback_start вызывается корректно
```

---

### 🔧 Исправление: Проверка hasattr(locals(), ...) в skipDataCollection

Исправлена некорректная проверка переменных в обработчике кнопок пропуска сбора данных.

#### Изменения:

- **До:** `hasattr(locals(), 'variable')` — некорректная проверка
- **После:** `'variable' in locals()` — безопасная проверка через оператор `in`

#### Файлы:

- `client/lib/skipDataCollection.ts` — исправлена проверка существования переменных

---

## 📊 Статистика изменений

| Категория | Файлов | Строк добавлено | Строк удалено |
|-----------|--------|-----------------|---------------|
| Исправление генератора | 1 | 5 | 11 |
| Исправление skipDataCollection | 1 | 1 | 1 |
| Документация | 1 | 217 | 0 |
| **Всего** | **3** | **223** | **12** |

---

## 🔧 Технические детали

### Изменения в `newgenerateInteractiveCallbackHandlersWithConditionalMessagesMultiSelectAndAutoNavigation.ts`

**До:**
```typescript
if (nodeId === 'start') {
    if (isLoggingEnabled()) console.log(`Пропускаем создание дублированной функции для узла ${nodeId}`);
    return; // Пропускаем создание дублированной функции
}
```

**После:**
```typescript
// ИСПРАВЛЕНИЕ: Создаем handle_callback_* для ВСЕХ узлов включая 'start'
// start_handler обрабатывает команду /start (message handler)
// handle_callback_start обрабатывает навигацию через callback (callback handler)
// Это разные функции, поэтому нужно создавать handle_callback_start для навигации
if (isLoggingEnabled()) console.log(`Создаем handle_callback_${nodeId} для узла ${nodeId} (включая start для навигации)`);
```

### Изменения в `skipDataCollection.ts`

**До:**
```python
if hasattr(locals(), 'skip_button_target'):
```

**После:**
```python
if 'skip_button_target' in locals():
```

---

## 🧹 Дополнительные исправления

### Обработчики навигации:

- ✅ **`handle_user_input.ts`** — корректная генерация вызовов `handle_callback_*` для всех узлов
- ✅ **`generateNodeNavigation.ts`** — проверка существования целевого узла перед вызовом обработчика
- ✅ **`skipDataCollection.ts`** — безопасная проверка через `in globals()` перед вызовом обработчика

---

## 🎨 UI/UX улучшения

1. **Стабильность** — устранена критическая ошибка навигации
2. **Надёжность** — все callback-обработчики генерируются корректно
3. **Логирование** — улучшено логирование создания обработчиков

---

## 🚀 Установка

Если вы используете локальную копию проекта:

```bash
# Обновите проект
git pull origin main

# Установите зависимости
npm install

# Соберите проект
npm run build

# Запустите проект
npm run start
```

### Для пользователей с существующими ботами:

```bash
# Перегенерируйте код бота в редакторе
# Откройте проект → Вкладка "Бот" → Кнопка "Сгенерировать"

# После генерации ошибка handle_callback_start исчезнет
```

---

## 📝 Примечания

- ✅ **Обратная совместимость** — все изменения обратно совместимы
- ✅ **Тестирование** — изменения протестированы на корректную работу
- 📦 **Зависимости** — не требуются новые зависимости
- ⚠️ **Важно:** После обновления перебалансируйте всех ботов для применения исправлений

---

## 🔗 Ссылки

- [Исходный код релиза](https://github.com/fedorabakumets/telegram-bot-builder/tree/v1.4.3)
- [Сравнение изменений](https://github.com/fedorabakumets/telegram-bot-builder/compare/v1.4.2...v1.4.3)
- [Полный список коммитов](https://github.com/fedorabakumets/telegram-bot-builder/commits/v1.4.3)

---

## 📋 Чек-лист обновления

- [x] Исправлена генерация `handle_callback_start` для узла `start`
- [x] Исправлена проверка `hasattr(locals(), ...)` в `skipDataCollection`
- [x] Обновлена документация релиза
- [x] Изменения закоммичены в `main`
- [x] Изменения отправлены на GitHub

---

## 🐛 Известные проблемы

Нет известных проблем в этой версии.

---

**Полный список изменений:** [Коммиты v1.4.2...v1.4.3](https://github.com/fedorabakumets/telegram-bot-builder/commits/main)
