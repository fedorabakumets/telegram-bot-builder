# Документация компонентов

Полная документация всех компонентов библиотеки с примерами использования.

## 🧩 Атомарные компоненты (Atoms)

### Button

Универсальная кнопка с поддержкой различных вариантов оформления, размеров, состояний загрузки и иконок.

**Возможности:**
- Множественные варианты (primary, secondary, outline, ghost, destructive, link)
- Размеры (sm, md, lg, icon)
- Состояние загрузки с индикатором
- Поддержка иконок до и после текста
- Полиморфный рендеринг через asChild
- Оптимизация производительности с React.memo

**Примеры использования:**

```tsx
// Базовая кнопка
<Button variant="primary" size="md">
  Нажми меня
</Button>

// Кнопка с иконкой
<Button icon={<Icon name="fa-solid fa-plus" />}>
  Добавить элемент
</Button>

// Кнопка в состоянии загрузки
<Button loading disabled>
  Сохранение...
</Button>

// Рендер как ссылка
<Button asChild>
  <a href="/profile">Перейти в профиль</a>
</Button>
```

### Input

Поле ввода с поддержкой различных состояний, размеров и иконок.

**Возможности:**
- Варианты (default, error, success) и размеры (sm, md, lg)
- Поддержка иконок в начале и конце поля
- Состояние загрузки с индикатором
- Интеграция сообщений об ошибках и успехе
- Оптимизация производительности с React.memo

**Примеры использования:**

```tsx
// Базовое поле ввода
<Input placeholder="Введите текст..." />

// Поле с ошибкой
<Input error="Это поле обязательно" placeholder="Email" />

// Поле с иконками
<Input 
  startIcon={<Icon name="fa-solid fa-search" />}
  placeholder="Поиск..."
/>

// Поле в состоянии загрузки
<Input loading placeholder="Поиск..." />
```

### Icon

Универсальный компонент иконок с поддержкой Font Awesome и кастомных SVG.

**Возможности:**
- Поддержка Font Awesome иконок
- Кастомные SVG элементы
- Множественные размеры и цвета
- Доступность с ARIA атрибутами
- Декоративный режим для скрытия от скринридеров

**Примеры использования:**

```tsx
// Font Awesome иконка
<Icon name="fa-solid fa-user" size="lg" color="primary" aria-label="Профиль пользователя" />

// Кастомная SVG иконка
<Icon size="md" color="success" aria-label="Успех">
  <svg viewBox="0 0 24 24" fill="currentColor">
    <path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/>
  </svg>
</Icon>

// Декоративная иконка
<Icon name="fa-solid fa-star" decorative />
```

### Typography

Компонент типографики для консистентного оформления текста.

**Возможности:**
- Семантические HTML элементы на основе варианта
- Полная шкала типографики из дизайн-системы
- Настраиваемые цвета, выравнивание и вес шрифта
- Переопределение HTML элемента через проп 'as'

**Примеры использования:**

```tsx
// Семантические заголовки
<Typography variant="h1">Главный заголовок страницы</Typography>
<Typography variant="h2">Заголовок секции</Typography>

// Текст с кастомным оформлением
<Typography variant="body" color="muted" align="center">
  Центрированный приглушенный текст
</Typography>

// Переопределение семантического элемента
<Typography variant="h2" as="h1">
  Визуально h2, семантически h1
</Typography>
```

### Label

Расширенный компонент метки с поддержкой обязательных полей и сообщений.

**Возможности:**
- Размеры, веса и цвета из дизайн-системы
- Индикатор обязательного поля
- Поддержка описания и сообщений об ошибках
- Построен на Radix UI Label для доступности
- Автоматическая корректировка цвета для состояний ошибки

**Примеры использования:**

```tsx
// Базовая метка
<Label htmlFor="email">Email адрес</Label>

// Обязательное поле
<Label htmlFor="password" required>
  Пароль
</Label>

// Метка с описанием
<Label 
  htmlFor="username" 
  description="Должно быть не менее 3 символов"
>
  Имя пользователя
</Label>

// Метка с ошибкой
<Label 
  htmlFor="email" 
  error="Введите корректный email адрес"
>
  Email
</Label>
```

## 🔗 Молекулярные компоненты (Molecules)

### SearchBox

Поиск с автодополнением, дебаунсингом и фильтрацией.

**Возможности:**
- Дебаунсинг ввода для оптимизации производительности
- Автодополнение с выпадающим списком
- Навигация с клавиатуры (стрелки, Enter, Escape)
- Функциональность кнопки очистки
- Поддержка состояния загрузки
- Настраиваемая фильтрация предложений

**Примеры использования:**

```tsx
// Базовый поиск
<SearchBox
  placeholder="Поиск пользователей..."
  onChange={(value, debouncedValue) => console.log(value, debouncedValue)}
  onSearch={(value) => console.log('Поиск:', value)}
/>

// С автодополнением
<SearchBox
  suggestions={['React', 'Vue', 'Angular', 'Svelte']}
  onSuggestionSelect={(suggestion) => console.log('Выбрано:', suggestion)}
  debounceMs={500}
/>

// Состояние загрузки
<SearchBox
  loading
  placeholder="Поиск..."
  value="поисковый запрос"
/>
```

### StatCard

Карточка статистики для отображения ключевых метрик с индикаторами изменений.

**Возможности:**
- Множественные варианты и размеры из дизайн-системы
- Поддержка иконок (Font Awesome или кастомные)
- Индикаторы изменений с состояниями увеличения/уменьшения/нейтрально
- Состояние загрузки с анимацией скелетона
- Интерактивный режим с эффектами наведения
- Настраиваемое форматирование значений

**Примеры использования:**

```tsx
// Базовая карточка статистики
<StatCard 
  title="Всего пользователей" 
  value={1234} 
  iconName="fa-solid fa-users"
/>

// Карточка с индикатором изменений
<StatCard
  title="Доход"
  value="$45,678"
  subtitle="За этот месяц"
  change={{
    value: "+12.5%",
    type: "increase",
    label: "по сравнению с прошлым месяцем"
  }}
  variant="success"
  iconName="fa-solid fa-dollar-sign"
/>

// Интерактивная карточка в состоянии загрузки
<StatCard
  title="Активные сессии"
  value={0}
  loading
  interactive
  onClick={() => console.log('Карточка нажата')}
/>
```

### UserAvatar

Аватар пользователя с поддержкой изображений, инициалов и индикатора статуса.

**Возможности:**
- Множественные размеры и формы из дизайн-системы
- Изображение с fallback на инициалы или иконку
- Индикатор онлайн статуса
- Автоматическая генерация цвета на основе имени
- Состояние загрузки с анимацией скелетона
- Режим кликабельности с эффектами наведения

**Примеры использования:**

```tsx
// Базовый аватар с изображением
<UserAvatar 
  name="Иван Иванов" 
  src="/avatars/ivan.jpg" 
/>

// Аватар с fallback на инициалы
<UserAvatar 
  name="Мария Петрова" 
  size="lg"
  shape="rounded"
/>

// Аватар с индикатором статуса
<UserAvatar
  name="Алексей Сидоров"
  status="online"
  showStatus
  clickable
  onClick={() => console.log('Аватар нажат')}
/>

// Загружающийся аватар
<UserAvatar loading size="xl" />
```

### FormField

Универсальное поле формы для консистентного оформления полей ввода.

**Возможности:**
- Консистентное оформление label, input и helper text
- Поддержка индикаторов обязательных полей
- Состояния ошибки, успеха и описания
- Горизонтальная и вертикальная ориентация
- Полная доступность с ARIA атрибутами
- Интеграция с любыми элементами форм

**Примеры использования:**

```tsx
// Базовое поле формы с input
<FormField
  label="Email адрес"
  id="email"
  required
  description="Мы никогда не поделимся вашим email"
>
  <Input 
    id="email"
    type="email" 
    placeholder="Введите email"
  />
</FormField>

// Поле с ошибкой
<FormField
  label="Пароль"
  id="password"
  required
  error="Пароль должен содержать минимум 8 символов"
>
  <Input 
    id="password"
    type="password"
    error="Пароль должен содержать минимум 8 символов"
  />
</FormField>

// Горизонтальное поле формы
<FormField
  label="Рассылка"
  id="newsletter"
  orientation="horizontal"
  description="Получать обновления о новых функциях"
>
  <input 
    id="newsletter"
    type="checkbox"
    className="rounded border-gray-300"
  />
</FormField>
```

## 🏗️ Организмы (Organisms)

### UserCard

Комплексная карточка пользователя с отображением информации и настраиваемыми действиями.

**Возможности:**
- Компактный и детальный варианты для разных случаев использования
- Отображение аватара с индикатором статуса
- Настраиваемые действия (кнопки)
- Интерактивность с обработкой кликов
- Состояние загрузки со скелетоном
- Поддержка кастомного контента

**Примеры использования:**

```tsx
<UserCard
  user={{
    id: '1',
    name: 'Иван Иванов',
    email: 'ivan@example.com',
    avatar: '/avatars/ivan.jpg',
    role: 'Администратор',
    status: 'online'
  }}
  variant="detailed"
  actions={[
    { label: 'Редактировать', onClick: () => {}, variant: 'primary' },
    { label: 'Удалить', onClick: () => {}, variant: 'destructive' }
  ]}
/>
```

## 🎨 UI Компоненты

### Card

Контейнер для группировки связанного контента.

**Примеры использования:**

```tsx
<Card>
  <CardHeader>
    <CardTitle>Заголовок карточки</CardTitle>
    <CardDescription>Описание карточки</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Основной контент карточки</p>
  </CardContent>
  <CardFooter>
    <Button>Действие</Button>
  </CardFooter>
</Card>
```

### Badge

Значок для отображения статусов, меток и категорий.

**Примеры использования:**

```tsx
// Базовый значок
<Badge>Новый</Badge>

// Различные варианты
<Badge variant="secondary">В процессе</Badge>
<Badge variant="destructive">Ошибка</Badge>
<Badge variant="outline">Черновик</Badge>

// С числами
<Badge>99+</Badge>
```

### Alert

Компонент уведомлений для отображения важных сообщений.

**Примеры использования:**

```tsx
// Базовое уведомление
<Alert>
  <AlertTitle>Внимание!</AlertTitle>
  <AlertDescription>
    Это важное уведомление для пользователя.
  </AlertDescription>
</Alert>

// Деструктивное уведомление с иконкой
<Alert variant="destructive">
  <ExclamationTriangleIcon className="h-4 w-4" />
  <AlertTitle>Ошибка!</AlertTitle>
  <AlertDescription>
    Произошла ошибка при сохранении данных.
  </AlertDescription>
</Alert>
```

## 📚 Дополнительные ресурсы

- **Storybook** - Интерактивная документация: `npm run storybook`
- **Тесты** - Запуск тестов: `npm run test`
- **Типы** - Все компоненты полностью типизированы с TypeScript
- **Доступность** - Все компоненты следуют стандартам WCAG
- **Производительность** - Оптимизированы с React.memo и кастомными сравнениями пропов

## 🔄 Обновления документации

Документация автоматически генерируется из JSDoc комментариев в коде. При изменении компонентов обновите соответствующие JSDoc комментарии и регенерируйте документацию:

```bash
npm run docs:generate
```