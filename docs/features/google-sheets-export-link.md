# Экспорт в Google Таблицы - Постоянная ссылка

## Обзор изменений

Добавлена возможность отображения постоянной ссылки на последнюю экспортированную Google Таблицу во вкладке "База данных пользователей".

## Изменённые файлы

### 1. `shared/schema.ts`
Добавлены новые поля в таблицу `bot_projects`:
- `lastExportedGoogleSheetId` - ID последней экспортированной Google Таблицы
- `lastExportedGoogleSheetUrl` - Полный URL таблицы
- `lastExportedAt` - Дата и время последнего экспорта

### 2. `server/google-sheets/export-metadata.ts` (новый файл)
Модуль для управления метаданными экспорта:
- `saveExportMetadata(projectId, spreadsheetId)` - сохранение метаданных
- `getExportMetadata(projectId)` - получение метаданных

### 3. `server/routes/setupProjectRoutes.ts`
Обновлён маршрут экспорта для сохранения метаданных после успешного экспорта.

### 4. `client/src/components/editor/user-database-panel.tsx`
Добавлено отображение ссылки на последнюю экспортированную таблицу с датой экспорта.

### 5. `client/src/lib/storage/local-storage.ts`
Обновлены типы и функции для работы с новыми полями проекта в localStorage.

### 6. `migrations/0001_add_google_sheet_export_fields.sql`
Файл миграции для добавления новых полей в базу данных.

### 7. `scripts/apply-google-sheets-migration.ts`
Скрипт для применения миграции.

## Как это работает

1. При экспорте данных в Google Таблицы метаданные автоматически сохраняются в базу данных проекта
2. На вкладке "База данных пользователей" отображается:
   - Ссылка на последнюю экспортированную таблицу
   - Дата и время последнего экспорта
3. При повторном экспорте данные обновляются

## Визуальное отображение

После успешного экспорта над таблицей пользователей появляется зелёная плашка:
```
📊 Последний экспорт: [Открыть Google Таблицу]    17 фев, 14:30
```

## Применение миграции

Миграция уже применена автоматически через скрипт. Для ручного применения:

```bash
npx tsx --tsconfig tsconfig.node.json scripts/apply-google-sheets-migration.ts
```

Или напрямую через SQL:
```sql
ALTER TABLE "bot_projects" 
ADD COLUMN "last_exported_google_sheet_id" text,
ADD COLUMN "last_exported_google_sheet_url" text,
ADD COLUMN "last_exported_at" timestamp;
```
