/**
 * Карточки фич на главной больше не используются —
 * контент перенесён в pages/index.tsx.
 * Файл оставлен пустым-заглушкой, чтобы не ломать импорты при очистке шаблона.
 */
export default function HomepageFeatures() {
  return null;
}
